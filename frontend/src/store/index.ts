/**
 * Global Zustand store placeholder.
 *
 * Sprint 1: No state needed — health page is stateless.
 *
 * Sprint 2 will add:
 * - AuthStore: currentUser, isAuthenticated, accessToken expiry
 *
 * Sprint 3+ will add:
 * - RepositoryStore: connected repos, indexing status
 * - UIStore: sidebar state, active theme
 */

// Reserved for Sprint 2
export {}
