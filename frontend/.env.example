# VITE_API_BASE_URL is not needed — Vite proxies /api → backend in dev
# Add frontend-specific public variables here (prefix with VITE_)

# App branding
VITE_APP_NAME=CodeSage AI
VITE_APP_VERSION=1.0.0-SNAPSHOT
